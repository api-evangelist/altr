{
  "opencollection": "1.0.0",
  "info": {
    "name": "ALTR Management SSO API",
    "version": "12.25.1"
  },
  "request": {
    "auth": {
      "type": "basic",
      "username": "{{username}}",
      "password": "{{password}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "sso",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Enables SSO for all identites in the same organization as the identity making the request",
            "type": "http"
          },
          "http": {
            "method": "PUT",
            "url": "https://altrnet.live.altr.com/api/saml/v1/enable",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Enables SSO for all identites in the same organization as the identity making the request"
        }
      ]
    }
  ],
  "bundled": true
}